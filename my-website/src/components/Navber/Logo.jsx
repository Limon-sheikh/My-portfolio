


const Logo = ({className}) => {
    return (
        <div>
            <a href="/" className={`text-2xl md:text-3xl xl:text-4xl font-bold ${className}`}>Limon</a>
        </div>
    );
};

export default Logo;